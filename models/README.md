# Generated models

`felidae.model` and `FelidaeSentencePieceIds.h` are generated by the
`felidae_sentencepiece_model` CMake target. They are intentionally outside
`build/` so that build output contains only build state and executables.
