# Generated models

`felidae.model` and `FelidaeSentencePieceIds.h` are generated by the
`felidae_sentencepiece_model` CMake target. Versioned LibTorch GRU artifacts
and manifests also belong in this directory. They are intentionally outside
`build/`, which is reserved for CMake state and build products.
