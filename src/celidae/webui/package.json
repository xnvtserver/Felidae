{
  "name": "celidae-webui",
  "private": true,
  "version": "1.0.0",
  "description": "Celidae's UI assets. Run 'npm run generate' after changing template.html, CSS, or a dependency to regenerate the runtime template and stylesheet.",
  "scripts": {
    "generate": "node generate-template.js"
  },
  "dependencies": {
    "cytoscape": "^3.30.2",
    "echarts": "^6.1.0",
    "heroicons": "^2.1.5"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
