# Native console stdlib declarations. Bodies are implemented by the native/runtime bridge.

console.readLine() => ().
console.input() => ().
console.input(print: string) => ().
console.inputNumber() => ().
console.inputNumber(print: string) => ().
console.write(value: any) => ().
console.writeLine(value: any) => ().
